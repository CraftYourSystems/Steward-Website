# URL of the Steward Admin Panel (where the Login button links to)
# Local dev:    http://localhost:3000
# Production:   set this in Vercel → Settings → Environment Variables
VITE_ADMIN_URL=http://localhost:3000
